<html>
    <body>
        <table>
            <thead>
                <tr>
                    <td>id</td>
                    <td>nome</td>
                    <td>descricao</td>
                    <td>preco</td>
                </tr>
            </thead>
            <tbody>
                <% for(var i=0; i<lista.length; i++) {%>
                <tr>
                    <td><%= lista[i].id %></td>
                    <td><%= lista[i].titulo %></td>
                    <td><%= lista[i].preco %></td>
                    <td><%= lista[i].descricao%></td>
                </tr>
                <% } %>
            </tbody>
        </table>
    </body>
</html>
